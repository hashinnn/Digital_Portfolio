# Port the Express server listens on (Render sets this automatically)
PORT=8080

# Optional: enable real email delivery for the contact form.
# Leave these blank and messages are logged to the server console instead.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
CONTACT_TO=hasinirg7@gmail.com
